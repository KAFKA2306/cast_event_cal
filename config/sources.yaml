version: 3
http:
  timeout_seconds: 25
window:
  past_days: 1
  future_days: 120
sources:
  - name: repository_manual_events
    type: manual_json
    enabled: true
    path: data/manual_events.json

  - name: vrchat_calendar_discovery
    type: manual_json
    enabled: true
    path: data/discovered_events.json

  - name: x_curated_events
    type: manual_json
    enabled: true
    path: data/x_events.json

# 公開ICS/JSONフィードは以下の形式で追加する。
# - name: community_ics
#   type: ics
#   enabled: true
#   url: https://example.com/calendar.ics
# - name: community_json
#   type: json
#   enabled: true
#   url: https://example.com/events.json
#   items_path: events
