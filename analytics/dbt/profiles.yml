cast_event_cal_read_model:
  target: ci
  outputs:
    ci:
      type: sqlite
      threads: 1
      database: "read_model"
      schema: "main"
      schemas_and_paths:
        main: "{{ env_var('DBT_ANALYTICS_DB') }}"
        canonical: "{{ env_var('DBT_CANONICAL_DB') }}"
      schema_directory: "{{ env_var('DBT_SCHEMA_DIR') }}"
