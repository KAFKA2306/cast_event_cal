name: Cleanup quality-view branch

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/cleanup-pages-uiux-quality-view.yml

permissions:
  contents: write

concurrency:
  group: cleanup-pages-uiux-quality-view
  cancel-in-progress: false

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Delete completed branch
        run: |
          set -euo pipefail
          branch='agent/pages-uiux-quality-view'
          if git ls-remote --exit-code --heads origin "refs/heads/$branch" >/dev/null 2>&1; then
            git push origin --delete "$branch"
          fi
      - name: Remove one-time cleanup workflow
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          rm .github/workflows/cleanup-pages-uiux-quality-view.yml
          git add -A
          git commit -m 'chore: remove completed UIUX branch cleanup [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
