name: Yahoo v1.8 intermediate materializer retired

on:
  pull_request:
    branches: [main]
    paths:
      - config/yahoo-v18-trigger.txt

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by materialize-yahoo-v18-final.yml'
