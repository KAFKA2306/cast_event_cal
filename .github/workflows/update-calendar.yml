name: Update calendar and deploy Pages

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  maintenance-window:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Repository consolidation in progress; use workflow_dispatch after completion.'
